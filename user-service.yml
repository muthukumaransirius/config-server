server:
  port: 8001

spring:
  application:
    name: user-service
  zipkin:
    base-url: http://127.0.0.1:9411/
  datasource:
    url: jdbc:mysql://localhost:3306/users
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
